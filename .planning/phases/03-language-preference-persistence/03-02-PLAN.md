---
phase: 03-language-preference-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/app/components/settings-modal/sections/appearance/appearance.ts
  - src/app/components/settings-modal/sections/appearance/appearance.html
  - src/app/components/settings-modal/sections/appearance/appearance.spec.ts
requirements:
  - LANG-01
autonomous: true

must_haves:
  truths:
    - "User sees a language dropdown in the Appearance & Language settings section"
    - "Dropdown shows two options: English and Deutsch"
    - "Selecting a language immediately switches the UI language"
    - "The dropdown reflects the current active language on load"
  artifacts:
    - path: "src/app/components/settings-modal/sections/appearance/appearance.ts"
      provides: "Language dropdown integrated into AppearanceSection component"
      contains: "LanguageService"
    - path: "src/app/components/settings-modal/sections/appearance/appearance.html"
      provides: "Language selector UI with dropdown below theme card"
      contains: "select"
  key_links:
    - from: "src/app/components/settings-modal/sections/appearance/appearance.ts"
      to: "src/services/language.service.ts"
      via: "inject(LanguageService) and setLanguage() call"
      pattern: "languageService.setLanguage"
---

<objective>
Add a language selector dropdown to the Appearance & Language settings section, wired to LanguageService for immediate language switching and persistence.

Purpose: Delivers the user-facing language selection UI (D-01) that triggers the save behavior (D-03) implemented in Plan 01's LanguageService.

Output: Language dropdown in settings that shows English/Deutsch, reflects current preference, and switches language on selection.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-language-preference-persistence/03-CONTEXT.md
@.planning/phases/03-language-preference-persistence/03-01-SUMMARY.md
@src/app/components/settings-modal/sections/appearance/appearance.ts
@src/app/components/settings-modal/sections/appearance/appearance.html
@src/app/components/settings-modal/sections/appearance/appearance.css
@src/services/language.service.ts
</context>

<interfaces>
<!-- Key contracts from Plan 01 output -->

From src/services/language.service.ts (created in Plan 01):
```typescript
export type AppLocale = 'en' | 'de';

@Injectable({ providedIn: 'root' })
export class LanguageService {
  readonly language: Signal<AppLocale>; // current active language
  initialize(): void;
  setLanguage(lang: AppLocale): void; // switches UI + saves localStorage + fires backend update
}
```

From src/app/components/settings-modal/sections/appearance/appearance.ts (current):
```typescript
@Component({
  selector: 'app-settings-appearance',
  imports: [],
  changeDetection: ChangeDetectionStrategy.OnPush,
  templateUrl: './appearance.html',
  styleUrl: './appearance.css',
})
export class AppearanceSection {
  private readonly themeService = inject(ThemeService);
  readonly themeOptions = [...];
  readonly selectedTheme = this.themeService.theme;
  setTheme(theme: ThemeOption) { ... }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add language dropdown to AppearanceSection component</name>
  <files>src/app/components/settings-modal/sections/appearance/appearance.ts, src/app/components/settings-modal/sections/appearance/appearance.html</files>
  <read_first>
    - src/app/components/settings-modal/sections/appearance/appearance.ts (current component structure)
    - src/app/components/settings-modal/sections/appearance/appearance.html (current template with theme card)
    - src/app/components/settings-modal/sections/appearance/appearance.css (existing styles)
    - src/services/language.service.ts (LanguageService API: language signal, setLanguage method)
  </read_first>
  <action>
    Modify appearance.ts:
    - Add import for LanguageService from '@services/language.service'
    - Add private readonly languageService = inject(LanguageService)
    - Add readonly languageOptions array: [{ value: 'en', label: 'English' }, { value: 'de', label: 'Deutsch' }]
    - Add readonly selectedLanguage = this.languageService.language (exposes the signal)
    - Add method setLanguage(lang: 'en' | 'de') that calls this.languageService.setLanguage(lang)

    Modify appearance.html — add a language section BELOW the existing theme card (after the closing div of settings-card for theme). Per D-01, use a simple dropdown/select:

    Add this block after the theme settings-card div:
    - A new div with class "settings-card"
    - h3 with class "settings-card-title" text "Language"
    - p with class "settings-card-desc" text "Choose your preferred display language."
    - A div with mt-4 containing a native select element:
      - select element with class "select select-bordered w-full max-w-xs" (DaisyUI select style consistent with app)
      - Bind value to selectedLanguage() signal
      - On change event, call setLanguage with the selected value
      - Two option elements: value="en" text "English", value="de" text "Deutsch"
      - Use Angular's property binding for selected state: [attr.selected] or compare in template

    Note: Since this is OnPush with signals, the select will reactively update when language signal changes.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | Select-String "error" -NotMatch | Select-Object -First 5; if ($LASTEXITCODE -eq 0) { echo "BUILD OK" } else { echo "BUILD FAILED" }</automated>
  </verify>
  <acceptance_criteria>
    - AppearanceSection imports and injects LanguageService
    - Template contains a select dropdown below the theme card
    - Dropdown has two options: "English" (value='en') and "Deutsch" (value='de')
    - Selecting an option calls languageService.setLanguage() with the chosen locale
    - The select reflects the current language signal value
    - Application builds without errors
  </acceptance_criteria>
  <done>Language dropdown is visible in settings, bound to LanguageService, and triggers immediate language switching on selection.</done>
</task>

<task type="auto">
  <name>Task 2: Add component test for language selector interaction</name>
  <files>src/app/components/settings-modal/sections/appearance/appearance.spec.ts</files>
  <read_first>
    - src/app/components/settings-modal/sections/appearance/appearance.spec.ts (existing test file for patterns)
    - src/app/components/settings-modal/sections/appearance/appearance.ts (component to test)
    - src/services/language.service.ts (service to mock)
  </read_first>
  <action>
    Update or create appearance.spec.ts with Vitest + TestBed tests:

    - Mock LanguageService with a writable signal for language and a vi.fn() for setLanguage
    - Mock ThemeService with a writable signal for theme
    - Test: "renders language dropdown with English and Deutsch options" — query the select element, verify two options with correct text and values
    - Test: "reflects current language in dropdown" — set mock language signal to 'de', detect change, verify select value is 'de'
    - Test: "calls setLanguage when selection changes" — simulate change event on select with value 'de', verify languageService.setLanguage was called with 'de'

    Use ComponentFixture pattern. Import the component, provide mocked services via TestBed.configureTestingModule.
  </action>
  <verify>
    <automated>pnpm test -- --run --reporter=verbose src/app/components/settings-modal/sections/appearance/appearance.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file has at least 3 tests covering rendering, current value display, and interaction
    - All tests pass
    - Tests mock LanguageService (not real backend calls)
  </acceptance_criteria>
  <done>AppearanceSection has passing tests confirming language dropdown renders correctly and triggers LanguageService on interaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI select → LanguageService | User input constrained to 'en'/'de' values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | select element value | mitigate | LanguageService.setLanguage validates input is 'en' or 'de' before processing; select options are hardcoded (not user-editable) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- pnpm build → no errors
- pnpm test -- --run src/app/components/settings-modal/sections/appearance/appearance.spec.ts → all pass
- Language dropdown visible in settings below theme card
- Selecting "Deutsch" switches UI language immediately
</verification>

<success_criteria>
- Language dropdown appears in Appearance & Language settings section below theme card
- Two options shown: "English" and "Deutsch"
- Selection triggers immediate language switch via LanguageService
- Dropdown reflects current language preference on load
- Component tests validate rendering and interaction
</success_criteria>

<output>
Create `.planning/phases/03-language-preference-persistence/03-02-SUMMARY.md` when done
</output>
